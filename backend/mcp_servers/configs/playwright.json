{
  "command": "npx",
  "args": [
    "-y", "@playwright/mcp@latest",
    "--browser", "chromium",
    "--caps", "storage",
    "--output-mode", "stdout",
    "--output-dir", "${BACKEND_ROOT}/.local/playwright-mcp"
  ]
}
